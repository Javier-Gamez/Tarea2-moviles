name: catalogo_ui
description: "Catálogo interactivo de elementos de interfaz"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  # Traducciones en español de los widgets de Material (selectores de fecha y hora, etc.).
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/paisaje.png
